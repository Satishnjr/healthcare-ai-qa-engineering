const DEFAULT_MULTI_AGENT_CONFIG = {
  MAX_GRAPH_STEPS: 24,
  MAX_TOOL_CALLS: 18,
  MAX_AGENT_RETRIES: 1,
  GRAPH_TIMEOUT_MS: 45000,
  CHECKPOINT_RETENTION: 100,
};

module.exports = {
  DEFAULT_MULTI_AGENT_CONFIG,
};

